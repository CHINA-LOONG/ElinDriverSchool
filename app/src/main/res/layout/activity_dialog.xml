<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_dialog"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/dialog_loading_bg"
    tools:context="com.elin.elindriverschool.activity.DialogActivity">
    <TextView
        android:layout_width="match_parent"
        android:layout_height="30dp"
        android:text="下线通知"
        android:textSize="18sp"
        android:textColor="@color/white"
        android:background="@drawable/version_bg"
        android:gravity="center"/>
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:padding="8dp"
        android:textColor="@color/dk_gray"
        android:text="您的账号已从其它设备登录。如非本人操作，请及时修改密码"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="10dp">
        <Button
            android:id="@+id/btn_exit"
            android:layout_width="wrap_content"
            android:layout_height="30dp"
            android:text="退出"
            android:background="@drawable/select_border_login"
            android:textColor="@color/white" />
        <Button
            android:id="@+id/btn_login_again"
            android:layout_width="wrap_content"
            android:layout_height="30dp"
            android:text="重新登录"
            android:background="@drawable/select_border_login"
            android:textColor="@color/white"
            android:layout_marginLeft="30dp"/>
    </LinearLayout>
</LinearLayout>
